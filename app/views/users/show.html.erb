<!--%= @users.inspect %-->

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
  </tr>
   <tr>
     <td><%= @user.name%></td>
     <td><%= @user.email%></td>
     <td><%= link_to "Edit", edit_user_path(@user)%>
   </tr>
</table>
<%= link_to "Home" , users_path%>
